HF_TOKEN=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
UPSTAGE_API_KEY=
PINECONE_API_KEY=

# 임베딩 모델 오버라이드 (기본값은 src/config.py의 DEFAULT_CONFIG["embed_model"] = BAAI/bge-m3).
# 다른 임베딩으로 만든 vect/ 벡터스토어를 쓰려면 반드시 그 임베딩으로 맞춰서 지정할 것 --
# 임베딩이 안 맞으면 "Collection expecting embedding with dimension of N, got M" 에러 발생.
# LOCAL_EMBEDDING_MODEL=BAAI/bge-m3
