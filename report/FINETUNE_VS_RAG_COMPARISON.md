# 파인튜닝 vs RAG 비교 실험

**"멍청한 모델을 파인튜닝하면 RAG가 필요 없어지는가?"에 대한 실증 답변**

## 배경

프로젝트 진행 중 "RAG 대신 그냥 작은 모델을 파인튜닝하면 되지 않나?"라는 질문이 나와서, 실제로 검증해봤습니다.

## 방법

1. `Qwen2.5-0.5B-Instruct`를 **LoRA**(경량 파인튜닝, RTX 4070 8GB에서 51초 학습)로 미세조정
2. 학습 데이터: 팀 공식 벤치마크 61문항 중 **실제 QA 53개만 사용** (트릭/가상 질문 8개는 학습에서 완전히 제외 — 모델이 "한 번도 못 본 질문"에 어떻게 반응하는지 보기 위함)
3. 학습된 모델을 두 그룹에 테스트:
   - (A) 학습 데이터에 포함됐던 질문
   - (B) 학습 데이터에 전혀 없던 트릭 질문 (홀드아웃)
4. 같은 트릭 질문을 RAG 파이프라인(`grounding_check` 포함)에도 물어봐서 비교

## 결과

| 질문 유형 | 파인튜닝 모델 | RAG(grounding_check) |
|---|---|---|
| **학습에 포함된 질문**("Pocket의 역할은?") | "Pocket는 휴대용으로 사용하는 기능입니다" — **오답** (정답: "2D 형상 공간을 파내는 기능") | (검색 기반이라 원문 그대로 인용 가능) |
| **학습에 없던 트릭 질문**("퀀텀 머시닝") | "Quantum Machining은... AI 기능입니다" — **환각 지속** | "The provided manual does not contain this information." — **정확히 거절** |

## 해석

### 1. 예상했던 결과: 트릭 질문에서 여전히 환각
파인튜닝은 "한 번도 본 적 없는, 존재하지 않는 개념"에 대해 여전히 그럴듯하게 지어냅니다. 이건 파인튜닝의 근본적 한계입니다 — **학습 데이터에 없는 질문에 "모른다"고 답하도록 만드는 것 자체가 파인튜닝만으로는 어려운 문제**입니다 (별도의 거부 학습 데이터·기법이 필요).

### 2. 예상 밖의 추가 발견: 적은 데이터로는 학습조차 제대로 안 됨
더 흥미로운 건, **학습 데이터에 포함됐던 질문조차 정답을 재현하지 못했다는 점**입니다. 53개 예제·8에폭(51초) 학습으로는 "Pocket"이라는 CATIA 전문 용어를 일반 단어(주머니)와 구분하지 못했습니다. 이는:
- 의미 있는 파인튜닝을 하려면 **훨씬 많은 데이터(수백~수천 건)와 학습 시간**이 필요하다는 뜻
- 반면 RAG는 **데이터 준비나 학습 과정 없이, 매뉴얼 PDF만 있으면 즉시 동작**

## 결론

| 항목 | 파인튜닝 | RAG |
|---|---|---|
| 새 질문(학습 안 한 것) 대응 | 여전히 환각 위험 | grounding_check로 매번 검증 |
| 적은 데이터로 신뢰성 확보 | 어려움(이번 실험에서 확인) | 문서만 있으면 바로 가능 |
| 매뉴얼 업데이트 시 | 재학습 필요 | 재인덱싱만 하면 됨 |
| 출처 인용 | 불가능 | 가능 (P.12 등) |

**"파인튜닝으로 성능을 올리자"는 방향은 이번 프로젝트(환각 방지·출처 인용이 핵심)에는 RAG보다 비효율적임을 실증으로 확인했습니다.** 이 실험 자체가 "왜 RAG를 선택했는가"에 대한 근거 자료입니다.

---

## 실험 2 — "그럼 문맥까지 같이 학습시키면 나아지나?" (RAG-태스크 파인튜닝)

실험 1은 "질문→정답 암기"였는데, 더 정교한 방식(실제 검색된 문맥을 학습 데이터에 포함시켜 "문맥 보고 답하는 스킬" 자체를 학습)이면 다를 수 있어서 추가로 검증했습니다.

### 방법
- 실제 QA 53개 + **트릭 질문 5개**를 학습에 사용, 이번엔 각 질문마다 **실제 벡터스토어에서 검색한 문맥(context)**을 붙여서 학습 (형식: `[문맥] + [질문] → [답변 또는 거절]`)
- 원래 트릭 질문 8개 중 학습에 안 쓴 **3개는 홀드아웃**으로 남겨둠
- 추가로 이 세상에 없던 **완전히 새로운 트릭 질문**("Emotion-Aware Design")도 별도로 만들어서 테스트 (학습 데이터 스타일과도 다른, 진짜 미지의 질문)

### 결과

| 테스트 | 결과 |
|---|---|
| 홀드아웃 트릭 질문(원래 8개 중 학습 안 한 것, "퀀텀 머시닝") | ✅ "The provided manual does not contain this information." — 정확히 거절 |
| 완전 신규 트릭 질문("Emotion-Aware Design") | ❌ "사용자가 감정에 따라 설계를 결정하는 기능입니다..." — 다시 환각 |
| 학습에 없던 정상 질문("Shaft 기능은?") | ⚠️ "원형의 모양의 기계에 사용되는 기계통로" — 방향은 비슷하나 부정확 |

### 해석
- **부분적으로 일반화 성공**: 학습에 쓴 트릭 질문(퀀텀 머시닝류 "SF 기술" 화법)과 비슷한 유형의 미지 질문은 정확히 거절함 → 순수 암기 방식(실험 1)보다는 확실히 나아짐
- **완전한 일반화는 실패**: 학습 데이터와 화법이 다른 새로운 유형("감정 인식")에는 여전히 뚫림 → 트릭 질문 5개만으로는 "모르면 거절한다"는 근본 원칙이 아니라 "이런 패턴이면 거절한다"는 얕은 규칙만 학습된 것으로 보임
- 실제 정답 추출 정확도도 완벽하지 않음 (Shaft 답변이 부정확)

### 3단계 종합 비교

| 방식 | 학습 필요? | 신규 트릭 질문 대응 |
|---|:---:|---|
| 실험 1: 순수 암기 파인튜닝 | 필요 | 0% (전부 환각) |
| 실험 2: RAG-태스크 파인튜닝 | 필요 (문맥 포함) | 부분적 (비슷한 유형만 방어) |
| **RAG + grounding_check** | **불필요** | **100%** (이번 세션에서 테스트한 모든 트릭 질문을 매번 정확히 거절) |

**결론**: 파인튜닝 방식을 아무리 정교화해도(문맥 포함 학습까지 해도) 소량 데이터로는 "완전한 신뢰성"에 도달하지 못했습니다. RAG + grounding_check는 학습 과정 없이 매번 실시간으로 검증하기 때문에 데이터 양과 무관하게 일관된 신뢰도를 보장합니다.

## 재현 방법

```bash
# 실험 1: 순수 암기 파인튜닝 (약 1분 소요, RTX 4070 8GB 기준)
python experiments/finetune_vs_rag_test.py
# 저장된 어댑터: experiments/lora_0.5b_catia/

# 실험 2: RAG-태스크 파인튜닝 (문맥 포함, 약 33분 소요 - 검색 문맥이 길어서 시퀀스가 김)
python experiments/finetune_rag_task.py
# 저장된 어댑터: experiments/lora_0.5b_rag_task/
# 홀드아웃 트릭 질문 목록: experiments/holdout_trick_questions.txt
```
